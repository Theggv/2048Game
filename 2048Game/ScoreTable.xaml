<UserControl x:Class="_2048Game.ScoreTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_2048Game"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="#CCAEA98C">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="textBlock" Text="Best results"
            FontSize="20" FontFamily="Verdana" FontWeight="Bold"
            Foreground="#FFFFFAEE" 
            HorizontalAlignment="Center" VerticalAlignment="Center"
            TextAlignment="Center" Opacity="0.9" Grid.Column="1" Grid.Row="1"/>
        <DataGrid Grid.Row="2" Grid.Column="1"
                  AutoGenerateColumns="False" CanUserResizeColumns="False" CanUserAddRows="False"
                  CanUserDeleteRows="False"> 
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nickname" Binding="{Binding Path=Name}"/>
                <DataGridTextColumn Header="Score" Binding="{Binding Path=Ball}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
